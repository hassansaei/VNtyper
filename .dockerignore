# =============================================================================
# VNtyper Docker Build Context Exclusions
# =============================================================================
# This file optimizes Docker build performance by excluding unnecessary files
# Best practices: https://docs.docker.com/build/building/context/#dockerignore-files
# =============================================================================

# Version Control
# =============================================================================
.git
.gitignore
.gitattributes
.github/

# CI/CD
# =============================================================================
.travis.yml
.gitlab-ci.yml
.circleci/
.codeclimate.yml

# Docker Files
# =============================================================================
docker-compose.yml
docker-compose.*.yml
Dockerfile
Dockerfile.*
.docker/
.dockerignore

# Python Artifacts
# =============================================================================
# Byte-compiled / optimized / DLL files
**/__pycache__/
**/*.py[cod]
**/*$py.class
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Testing & Coverage
# =============================================================================
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/
.pytype/

# VNtyper Test Data (CRITICAL - Saves 1.2GB+)
# =============================================================================
tests/
testing/
tests/data/
tests/data_anonymized/
tests/benchmark/

# Bioinformatics Data Files (CRITICAL - Saves 143GB+)
# =============================================================================
# Reference data is no longer a build input. docker/Dockerfile.base's `refs` stage fetches
# the published, checksummed bundle from berntpopp/vntyper-data at build time instead of
# reading it out of the build context, so nothing under reference/ needs to reach the
# daemon. Exclude the whole tree: a local checkout can have several GB installed there by
# `vntyper install-references`, and sending it as build context would only slow every
# build down for no benefit.
reference/
reference_old/

# User output and downloads
out/
output/
download/
results/

# Large bioinformatics file formats
*.bam
*.bam.bai
*.sam
*.cram
*.fastq
*.fastq.gz
*.fq
*.fq.gz
*.fa.gz
*.fasta.gz
*.vcf.gz
*.bcf

# Environment & IDE
# =============================================================================
# Virtual environments
.env
.venv/
venv/
ENV/
env/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*.swn
.ropeproject/
**/.ropeproject

# OS Files
# =============================================================================
.DS_Store
Thumbs.db
*.tmp
*.log
*.bak
*.backup
*~

# Documentation Build
# =============================================================================
docs/_build/
docs/.build/
site/

# Jupyter Notebooks
# =============================================================================
.ipynb_checkpoints/
**/.ipynb_checkpoints

# Project Specific
# =============================================================================
# Keep only essential code - exclude everything else
*.zip
*.tar
*.tar.gz
*.tgz
coverage.json

# Development tools
.mypy_cache/
.dmypy.json
dmypy.json

# Translations
*.mo
*.pot

# Database
*.db
*.sqlite
*.sqlite3

# Local clone of the upstream adVNTR source tree. It is in .gitignore, but Docker does
# NOT read .gitignore - without this line it was 46 MB of a 49 MB build context, and
# install_advntr.sh would collide with a context-copied tree.
adVNTR/

# Dev tooling caches and agent config - never needed in an image
.ruff_cache/
.benchmarks/
.claude/
AGENTS.md
CLAUDE.md

# Docs and workflow sources are not part of the runtime image
docs/
mkdocs.yml
.github/
